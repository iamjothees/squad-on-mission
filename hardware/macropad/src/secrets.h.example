#ifndef SECRETS_H
#define SECRETS_H

// Replace these with your actual network credentials and backend API details
const char* WIFI_SSID = "YOUR_WIFI_SSID";
const char* WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";

// Make sure this points to the local IP of your Laravel server running on port 8030
const char* API_URL = "http://192.168.x.x:8030/api/device";

// API_TOKEN is now automatically generated by the ESP32 on first boot
// and saved in its internal Preferences storage.

#endif
